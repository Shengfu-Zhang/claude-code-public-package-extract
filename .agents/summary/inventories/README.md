# Inventories

This directory is reserved for supporting inventories and future machine-generated indexes.

The current documentation set keeps the most useful inventory information inline in `../codebase_info.md` and `../index.md`.
