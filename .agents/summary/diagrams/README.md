# Diagrams

This directory is reserved for standalone diagram artifacts.

For this run, Mermaid diagrams are embedded directly inside the generated markdown documents so each diagram stays adjacent to its explanatory text.
