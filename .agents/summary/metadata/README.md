# Metadata

This directory is reserved for structured or machine-readable metadata that may be added in later refreshes.

For this run, the primary metadata lives inline in:

- `../index.md` for knowledge-base routing and retrieval guidance
- `../codebase_info.md` for repository scope and analysis caveats
